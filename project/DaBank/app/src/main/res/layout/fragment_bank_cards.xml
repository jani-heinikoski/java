<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/fragment_bank_cards_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/fragment_bank_cards_add_card"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="16dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fragment_bank_cards_add_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/fragment_bank_cards_add_card_header"
        android:layout_centerInParent="true"
        android:layout_margin="4dp"
        android:elevation="8dp"
        app:fabSize="normal"
        app:maxImageSize="56dp"
        app:srcCompat="@drawable/ic_add_circle"
        app:useCompatPadding="false" />

    <TextView
        android:id="@+id/fragment_bank_cards_add_card_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:text="@string/fragment_bank_cards_add_card_header_text"
        android:textColor="@color/colorPrimaryText"
        android:textSize="18sp"
        android:textStyle="italic" />


</RelativeLayout>